name: SERVEO-PUTTY-BAGLANTISI

on:
  workflow_dispatch:

jobs:
  terminal-ac:
    runs-on: ubuntu-latest
    steps:
      - name: Sifre ve SSH Ayarla
        run: |
          # Kullanıcı: runner | Sifre: 123456
          echo "runner:123456" | sudo chpasswd
          sudo sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo service ssh restart

      - name: Serveo ile Baglanti Kur
        run: |
          # Serveo üzerinden 22 portunu dışarı açıyoruz
          # serveo.net üzerinden bir tünel oluşturur
          ssh -o StrictHostKeyChecking=no -R 80:localhost:22 serveo.net > serveo.log 2>&1 &
          sleep 5
          
      - name: Adresi Al
        run: |
          echo "--------------------------------------------------"
          echo "👇👇 PUTTY BILGILERI 👇👇"
          echo "Host Name: serveo.net"
          echo "Kullanici: runner"
          echo "Sifre: 123456"
          echo "--------------------------------------------------"
          echo "Not: Eger baglantı koparsa workflow loglarını kontrol et."
          
      - name: Bekle
        run: sleep 21600
