name: OLUMSUZ-TMATE-TERMINAL 😈

on:
  workflow_dispatch:

jobs:
  terminal:
    runs-on: ubuntu-latest
    steps:
      - name: tmate Ac
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: false
          
      - name: SAKIN KAPANMA (6 SAAT)
        run: |
          echo "Terminal kapansa bile ben buradayim kanka!"
          sleep 21600
