name: TekPoyraz - CANLI TERMINAL 😈

on:
  workflow_dispatch:

jobs:
  terminal:
    runs-on: ubuntu-latest
    # Sadece 1 makine açıyoruz, kafan karışmasın
    steps:
      - name: Makineyi Hazırla ve Bekle
        uses: mxschmitt/action-tmate@v3
        with:
          # Bağlandığında terminalin kapanmaması için limit koymuyoruz
          limit-access-to-actor: false
